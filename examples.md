

### Легенда: Капкан для Data-инженера (Сценарий из ML)

Представьте, что дата-саентист разработал кастомную **функцию активации (или метрику скоринга)** для финансовой нейросети. Переменная $x$ — это сырой показатель финансовой активности клиента (баланс операций). 

Задумка инженера была красивой: 
> *«Если активность $x$ растет в плюс, моя формула должна плавно округлить рейтинг клиента до максимума (до $+1$). Если баланс уходит в минус, формула должна плавно опустить рейтинг до $-1$. Отличный симметричный нормализатор!»*

Инженер пишет код формулы $y = \frac{x}{\sqrt{x^2 + x}}$, запускает обучение на миллионе транзакций и уходит пить кофе. Через минуту сервер "падает" с критической ошибкой, а часть клиентов получает бесконечные штрафы.

Давайте проведем математическое расследование и выясним, почему этот код сломал систему.

---

### Полное исследование функции $y = \frac{x}{\sqrt{x^2 + x}}$

#### 1. Область определения $D(y)$ (Где алгоритм "живет")
Подкоренное выражение должно быть строго больше нуля (так как корень стоит еще и в знаменателе, сам ноль брать нельзя):
$x^2 + x > 0$
$x(x + 1) > 0$
Решаем методом интервалов: корни $0$ и $-1$. Подставляем контрольные точки, получаем, что функция существует только по краям.
**Вывод:** $D(y) = (-\infty; -1) \cup (0; +\infty)$.
* **Смысл для ИИ:** Это первая катастрофа. Между $-1$ и $0$ находится огромная "мертвая зона". Если финансовая активность клиента упадет всего на полпункта ($x = -0.5$), алгоритм попытается извлечь корень из отрицательного числа и выдаст `NaN`. Программа крашнется.

#### 2. Четность и нечетность (Симметрия)
Область определения не симметрична относительно нуля (например, $0.5$ в нее входит, а $-0.5$ — нет). 
**Вывод:** Функция общего вида. Ожидания инженера о красивой симметрии провалились.

#### 3. Точки пересечения с осями
* **С осью $Oy$ ($x = 0$):** Точка $x = 0$ выколота (не входит в $D(y)$). Пересечения нет.
* **С осью $Ox$ ($y = 0$):** Дробь равна нулю, когда числитель равен нулю. То есть $x = 0$. Но мы только что выяснили, что эту точку брать нельзя.
**Вывод:** График нигде не пересекает оси координат.

#### 4. Асимптоты (Границы и обрывы)
Исследуем края нашей области определения:
* **Правый обрыв (при $x \to 0$ справа):** $\lim_{x \to 0+} \frac{x}{\sqrt{x^2+x}} = \lim_{x \to 0+} \frac{\sqrt{x} \cdot \sqrt{x}}{\sqrt{x} \cdot \sqrt{x+1}} = \lim_{x \to 0+} \frac{\sqrt{x}}{\sqrt{x+1}} = \frac{0}{1} = 0$.
    Вертикальной асимптоты здесь нет. График просто начинается из пустой (выколотой) точки $(0; 0)$ и идет вправо.
* **Левый обрыв (при $x \to -1$ слева):**
    $\lim_{x \to -1-} \frac{x}{\sqrt{x(x+1)}} = \frac{-1}{\sqrt{(-1) \cdot (-0)}} = \frac{-1}{+0} = -\infty$.
    **Вертикальная асимптота: $x = -1$.**
* **Горизонтальные асимптоты (Бесконечность):**
    * $x \to +\infty$: $\lim_{x \to +\infty} \frac{x}{x\sqrt{1 + 1/x}} = 1$. Асимптота: **$y = 1$**.
    * $x \to -\infty$: Извлекая корень из $x^2$ для отрицательных чисел, мы получаем $|x| = -x$.
        $\lim_{x \to -\infty} \frac{x}{-x\sqrt{1 + 1/x}} = -1$. Асимптота: **$y = -1$**.
* **Смысл для ИИ:** Вторая катастрофа. Если баланс клиента приближается к $-1$ (например, $x = -1.01$), рейтинг улетает в $-\infty$. Модель выпишет клиенту бесконечный штраф за микроскопический долг.

#### 5. Первая производная (Монотонность)
Покажет, куда двигается алгоритм при градиентном спуске.
$$y' = \frac{1 \cdot \sqrt{x^2+x} - x \cdot \frac{2x+1}{2\sqrt{x^2+x}}}{x^2+x} = \frac{2(x^2+x) - x(2x+1)}{2(x^2+x)^{3/2}} = \frac{x}{2(x^2+x)^{3/2}}$$

Знаменатель всегда положителен. Знак производной зависит только от $x$:
* На интервале $(0; +\infty)$ имеем $x > 0 \Rightarrow y' > 0$. **Функция возрастает.**
* На интервале $(-\infty; -1)$ имеем $x < -1 \Rightarrow y' < 0$. **Функция убывает.**
**Критических точек нет** (числитель равен нулю только при $x=0$, а этой точки нет в области определения). Экстремумы отсутствуют.

#### 6. Вторая производная (Выпуклость)
Показывает форму кривой и наличие "капканов". После дифференцирования первой производной и упрощения получаем:
$$y'' = \frac{-x(4x + 1)}{4(x^2+x)^{5/2}}$$
* Для $x > 0$: выражение $-x(4x + 1)$ всегда отрицательное. Знак $y'' < 0$ (выпуклая вверх, форма купола).
* Для $x < -1$: $x$ — отрицательное, значит $(-x)$ — положительное. Скобка $(4x+1)$ — отрицательная (например, $4(-2)+1 = -7$). Плюс на минус дает минус. Знак $y'' < 0$ (выпуклая вверх).
**Вывод:** Весь график на обоих кусках строго выпуклый вверх (купол). Точек перегиба нет.

---

### Финальный вердикт для бизнеса
Математика доказала, что задумка инженера была провальной. График состоит из двух оторванных друг от друга кусков:
1.  **Правая ветвь ($x > 0$):** Идет из пустой точки $(0;0)$ плавно вверх, прижимаясь к "потолку" $y = 1$. (Эта часть работает как задумано).
2.  **Левая ветвь ($x < -1$):** Идет из $-\infty$ (возле вертикальной стены $x=-1$) и, убывая по дуге, прижимается к "полу" $y = -1$.
3.  **Между $0$ и $-1$:** Черная дыра.

Эта формула — отличный пример того, почему в Data Science нельзя использовать сложные алгебраические дроби с корнями без предварительного расчета их поведения на бумаге.

---

**Промт для слайда:**

> Создай дизайн слайда для разбора "Fail-кейса" в машинном обучении. Заголовок: "Ошибка проектирования метрики". В левой части слайда размести блок с формулой y = x / √(x² + x) и красным штампом "CRITICAL ERROR". В правой части крупно изобрази график этой функции. Детальная структура графика: декартова система; серая мертвая зона (заштрихованная область) между x=-1 и x=0. Правая ветвь графика (синяя линия) выходит из выколотой точки в начале координат и растет вправо до горизонтального пунктира y=1. Левая ветвь (красная линия) начинается из левого нижнего угла (от вертикального пунктира x=-1) и плавно спускается к горизонтальному пунктиру y=-1. Добавь стилизованные элементы предупреждений и логов ошибок сервера.




### Практика: Исследование параметрической функции от и до

**Легенда для ML-инженера:**
Представьте, что вы запустили обучение нейросети с двумя весами: $w_1$ (ось $X$) и $w_2$ (ось $Y$). Алгоритм градиентного спуска делает шаги, и время (номер эпохи обучения) мы обозначаем параметром $t$. 

Наша задача — нарисовать **траекторию обучения (путь алгоритма)**, зная только то, как каждый вес меняется во времени $t$:

$$\begin{cases} x = t^2 - 1 \\ y = t^3 - t \end{cases}$$

Давайте исследуем этот путь от начала и до конца.

---

#### Шаг 1. Область определения параметра $t$
Здесь $x(t)$ и $y(t)$ — обычные многочлены. В них нет корней, логарифмов или дробей.
* **Математика:** $t \in (-\infty; +\infty)$.
* **Смысл:** Мы можем отследить траекторию как в бесконечном будущем ($t \to +\infty$), так и в бесконечном прошлом ($t \to -\infty$).

#### Шаг 2. Симметрия кривой (Четность/Нечетность от $t$)
Проверяем, что будет, если заменить время $t$ на $-t$:
* $x(-t) = (-t)^2 - 1 = t^2 - 1 = x(t)$. (Координата $X$ четная — не меняется).
* $y(-t) = (-t)^3 - (-t) = -t^3 + t = -y(t)$. (Координата $Y$ нечетная — меняет знак на противоположный).
* **Смысл:** Раз при смене знака $t$ у нас $X$ остается тем же, а $Y$ отзеркаливается, значит **весь график идеально симметричен относительно горизонтальной оси $Ox$**. Это сильно упрощает нам жизнь: можно построить только верхнюю половину кривой, а нижнюю просто дорисовать как отражение в воде.

#### Шаг 3. Точки пересечения с осями координат
* **С осью $Oy$ (где $x = 0$):**
  $t^2 - 1 = 0 \Rightarrow t^2 = 1 \Rightarrow t = 1$ и $t = -1$.
  Подставляем эти $t$ в формулу для $y$:
  При $t = 1 \Rightarrow y = 1^3 - 1 = 0$. Точка **$(0; 0)$**.
  При $t = -1 \Rightarrow y = (-1)^3 - (-1) = -1 + 1 = 0$. Снова точка **$(0; 0)$**.
  *Инсайд:* Алгоритм прошел через одну и ту же точку пространства $(0;0)$ дважды в разное время ($t=1$ и $t=-1$). Значит, наша траектория пересекает саму себя, образуя **петлю**!

* **С осью $Ox$ (где $y = 0$):**
  $t^3 - t = 0 \Rightarrow t(t^2 - 1) = 0 \Rightarrow t = 0, t = 1, t = -1$.
  При $t = 1$ и $t = -1$ мы уже знаем, что координаты **$(0; 0)$**.
  А при $t = 0 \Rightarrow x = 0^2 - 1 = -1$. Точка **$(-1; 0)$**.

#### Шаг 4. Асимптоты
Поскольку при $t \to \infty$ обе координаты $x$ и $y$ тоже уходят в бесконечность, ни вертикальных, ни горизонтальных асимптот нет. Траектория ничем не ограничена.

#### Шаг 5. Первая производная (Монотонность и Касательные)
Нам нужно узнать, в каких точках алгоритм менял направление. Вычисляем производную $y$ по $x$:
1. Берем производные по параметру $t$: 
   $x'_t = 2t$
   $y'_t = 3t^2 - 1$
2. Собираем основную производную: 
   $$y'_x = \frac{y'_t}{x'_t} = \frac{3t^2 - 1}{2t}$$

Ищем **экстремумы** (где касательная строго горизонтальна, $y'_x = 0$):
Числитель равен нулю: $3t^2 - 1 = 0 \Rightarrow t^2 = \frac{1}{3} \Rightarrow t = \pm \frac{1}{\sqrt{3}} \approx \pm 0.57$.
Подставив эти $t$ в исходные уравнения, получим "вершины" нашей кривой по оси Y:
Точки **$(-\frac{2}{3}; -0.38)$** и **$(-\frac{2}{3}; +0.38)$**.

Ищем **вертикальные касательные** (где знаменатель $y'_x$ равен нулю, алгоритм летит строго вверх/вниз):
Знаменатель равен нулю: $2t = 0 \Rightarrow t = 0$.
Мы уже считали эту точку: это "нос" нашей петли с координатами **$(-1; 0)$**.

#### Шаг 6. Вторая производная (Выпуклость и форма)
Проверяем форму дуг. Формула второй производной для параметрической функции:
$$y''_{xx} = \frac{(y'_x)'_t}{x'_t}$$

Сначала найдем $(y'_x)'_t$, дифференцируя полученную ранее дробь $\frac{3t^2 - 1}{2t}$:
$$\left(\frac{3t^2 - 1}{2t}\right)'_t = \frac{(6t \cdot 2t) - (3t^2 - 1 \cdot 2)}{(2t)^2} = \frac{12t^2 - 6t^2 + 2}{4t^2} = \frac{6t^2 + 2}{4t^2} = \frac{3t^2 + 1}{2t^2}$$

Теперь делим это на $x'_t$ (то есть на $2t$):
$$y''_{xx} = \frac{3t^2 + 1}{4t^3}$$

**Анализ знаков:**
Числитель ($3t^2 + 1$) всегда строго положительный. Знак зависит только от $t^3$ в знаменателе:
* При $t > 0$: $y''_{xx} > 0$. Траектория **вогнутая** (форма чаши, улыбка).
* При $t < 0$: $y''_{xx} < 0$. Траектория **выпуклая** (форма купола, грустный смайлик).
* При $t = 0$: Происходит смена знака. Точка $(-1; 0)$ является **точкой перегиба**. Именно в этом месте купол плавно превращается в чашу.



---

### Финальная картина (Вывод для ИИ)
Если мы нарисуем эти точки на графике, мы увидим классическую кубику Чирнгауза (петлю). 
* Алгоритм прилетает из верхнего правого угла (из прошлого $t \to -\infty$).
* Спускается вниз, проходя через начало координат $(0;0)$ при $t=-1$.
* Делает красивый разворот в крайней левой точке $(-1; 0)$ при $t=0$ (точка перегиба).
* Снова проходит через начало координат $(0;0)$ при $t=1$.
* Улетает в нижний правый угол (в будущее $t \to +\infty$).

**Диагноз модели:** Траектория градиентного спуска сделала самопересекающуюся "мертвую петлю". В реальности это означает, что **Learning Rate (шаг обучения) был выбран слишком большим**. Алгоритм перелетел оптимальную точку, развернулся, пересек свой собственный след и ушел в бесконечность. Требуется калибровка шага!

### Практика: Датасет Титаника и Функция Сигмоиды

В знаменитом датасете Титаника мы решаем задачу **классификации**. Наша цель — предсказать, выжил пассажир ($y=1$) или погиб ($y=0$). 

Представьте, что мы уже собрали данные пассажира (класс каюты, цена билета, пол, возраст) и сжали их в единое число — **«Индекс выживаемости» ($x$)**. 
Например:
* $x = +5$ (богатая женщина из 1-го класса).
* $x = -4$ (пожилой мужчина из 3-го класса).

Если мы просто отдадим этот индекс линейной функции, она может выдать вероятность выживания "150%" или "-20%", что для бизнеса и здравого смысла — абсурд. Чтобы загнать любые числа в строгие рамки вероятности от 0% до 100%, в машинном обучении используется функция **Сигмоида (Sigmoid)**.

Давайте исследуем её математически и поймем, почему она идеально подходит для предсказания катастрофы Титаника.

**Наша функция вероятности выживания:**
$$y = \frac{1}{1 + e^{-x}}$$
Где $e \approx 2.71$ (число Эйлера).

---



[Image of sigmoid function graph]


#### Шаг 1. Область определения $D(y)$ (Кто может попасть в модель?)
* **Математика:** Экспоненту $e$ можно возводить в абсолютно любую степень (хоть в положительную, хоть в отрицательную, хоть в ноль). Знаменатель никогда не обратится в ноль, так как $e^{-x}$ всегда строго больше нуля.
  $D(y) = (-\infty; +\infty)$
* **Смысл для ИИ:** Модель «всеядна». Независимо от того, насколько экстремальные данные мы в нее загрузим (билет стоил миллион долларов или пассажиру 100 лет, что даст огромные значения $x$), код не сломается и функция спокойно обработает эти числа.

#### Шаг 2. Точки пересечения с осями координат (Базовая линия)
* **С осью $Oy$ ($x = 0$):**
  $y = \frac{1}{1 + e^0} = \frac{1}{1 + 1} = 0.5$
  Точка **$(0; 0.5)$**.
* **С осью $Ox$ ($y = 0$):**
  Дробь равна нулю только если числитель равен нулю. Но у нас в числителе железная единица. Пересечений с осью $Ox$ нет.
* **Смысл для ИИ:** Если пассажир абсолютно "средний" (его плюсы и минусы уравновесили друг друга, и индекс $x=0$), модель честно скажет: вероятность его выживания ровно 50% ($y=0.5$). Это идеальная точка равновесия.

#### Шаг 3. Асимптоты (Жесткие границы вероятности)
Посмотрим, что происходит на экстремальных краях.
* **Правая асимптота ($x \to +\infty$):**
  Если $x$ — огромное положительное число, то $e^{-\infty}$ стремится к нулю.
  $\lim_{x \to +\infty} \frac{1}{1 + 0} = 1$. Горизонтальная асимптота **$y = 1$**.
* **Левая асимптота ($x \to -\infty$):**
  Если $x$ — огромное отрицательное число, то $e^{-(-\infty)} = e^{+\infty}$, что дает бесконечность в знаменателе.
  $\lim_{x \to -\infty} \frac{1}{1 + \infty} = 0$. Горизонтальная асимптота **$y = 0$**.
* **Смысл для ИИ:** Это главная победа Сигмоиды. Как бы ни зашкаливал "Индекс выживаемости", вероятность плавно прижмется к 1 (100% шанс выжить), но никогда не превысит её. Аналогично, вероятность никогда не упадет ниже 0. Мы получили математически корректный процент!

#### Шаг 4. Первая производная (Монотонность)
Поймем, есть ли у графика скачки или экстремумы.
$$y' = \left( (1 + e^{-x})^{-1} \right)' = -1 \cdot (1 + e^{-x})^{-2} \cdot (e^{-x})' = \frac{e^{-x}}{(1 + e^{-x})^2}$$
* **Математика:** Экспонента всегда положительна. Знаменатель в квадрате всегда положителен. Значит, $y' > 0$ при любых $x$. Функция **строго возрастает** на всей числовой прямой. Критических точек и экстремумов нет.
* **Смысл для ИИ:** Логика не нарушается. Чем выше индекс пассажира ($x$), тем выше его шансы на спасение ($y$). График нигде не проваливается вниз: не бывает так, что добавление "хороших" факторов вдруг уменьшит вероятность выжить.

#### Шаг 5. Вторая производная (Выпуклость и Порог принятия решения)
После вычисления производной от первой производной (и небольших алгебраических преобразований), получается изящная формула:
$$y'' = y \cdot (1 - y) \cdot (1 - 2y)$$
Приравниваем к нулю, чтобы найти точку перегиба:
Так как $y$ всегда в пределах от 0 до 1, нулем может стать только последняя скобка:
$1 - 2y = 0 \Rightarrow 2y = 1 \Rightarrow y = 0.5$ (что соответствует $x = 0$).

* **Анализ знаков:**
  * При $x < 0$ (вероятность $y < 0.5$): вторая производная положительна ($y'' > 0$). График **вогнутый** (ускоряется вверх).
  * При $x > 0$ (вероятность $y > 0.5$): вторая производная отрицательна ($y'' < 0$). График **выпуклый** (замедляется, приближаясь к потолку).
* **Смысл для ИИ:** Точка перегиба $(0; 0.5)$ — это **Порог классификации (Decision Boundary)**. В этой точке функция меняет свое поведение. До этой точки каждый небольшой плюс к индексу пассажира дает огромный прирост к шансу выжить. Но после перевала (когда вероятность перевалила за 50%), рост замедляется (эффект убывающей полезности). Если вы уже на 90% в безопасности, покупка более дорогого билета почти ничего не изменит.

---

### Итог для аналитика
Проведя это классическое исследование, мы доказали, что Сигмоида — идеальный "переводчик" сырых данных Титаника в язык вероятностей. Она непрерывна, зажата между $0$ и $1$, логично растет и имеет четкий порог принятия решений ровно на $50\%$.

---

**Промт для слайда:**

> Создай макет слайда для презентации: "Математика классификации: Сигмоида на датасете Титаника". Раздели слайд на две зоны. Левая зона (Смысл): текст "x = Индекс пассажира (Класс, Пол, Возраст)", "y = Вероятность выживания (0% - 100%)". Ниже три буллита: "1. Нет ошибок NaN (D(y) бесконечна)", "2. Жесткие рамки (Асимптоты 0 и 1)", "3. Логичный рост (y' всегда > 0)". Правая зона (Визуализация): Нарисуй S-образный график (Сигмоиду) на декартовой плоскости. Ось X — "Индекс (от -5 до +5)". Ось Y — "Вероятность (от 0 до 1)". Ярко выдели горизонтальные асимптоты y=1 (зеленый пунктир) и y=0 (красный пунктир). В самом центре графика (координаты 0; 0.5) поставь светящийся маркер с надписью "Точка перегиба: Порог 50%". Цветовая гамма: строгий темно-синий фон, график голубого цвета, контрастные акценты для данных.
