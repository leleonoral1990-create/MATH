### Этап 1. Область определения функции $D(f)$

**Объяснение:**
Это множество всех значений переменной $x$, при которых функция «существует» (ее можно вычислить). Мы ищем «запретные зоны».

* **Дробь:** знаменатель не равен 0.
* **Корень четной степени:** подкоренное выражение $\ge 0$.
* **Логарифм:** аргумент $> 0$.

**Пример:**
Для функции $f(x) = \frac{5}{x - 3} + \sqrt{x + 1}$:

1. $x - 3 \neq 0 \Rightarrow x \neq 3$.
2. $x + 1 \ge 0 \Rightarrow x \ge -1$.
**Итог:** $D(f) = [-1; 3) \cup (3; +\infty)$.



### Этап 2. Область значений функции $E(f)$

**Объяснение:**
Область значений функции (множество значений) — это все возможные значения, которые может принимать зависимая переменная $y$ (функция) при переборе всех допустимых значений независимой переменной $x$ из области определения. Геометрически область значений представляет собой проекцию всего графика функции на вертикальную ось ординат ($Oy$).

В то время как область определения показывает, где график существует по горизонтали, область значений показывает, какие границы график имеет по вертикали.

Основные способы нахождения $E(f)$:

1. **Оценка алгебраических выражений:** использование базовых свойств (любой квадрат $\ge 0$, модуль $\ge 0$, корень $\ge 0$).
2. **Свойства тригонометрических функций:** использование встроенных ограничений (например, $-1 \le \sin(x) \le 1$).
3. **Использование производной:** нахождение абсолютных максимумов и минимумов функции на ее области определения.
4. **Выражение $x$ через $y$:** решение уравнения $y = f(x)$ относительно $x$ для поиска ограничений на $y$.

**Примеры:**

* **Пример 1 (Квадратичная функция):**
$f(x) = x^2 - 3$
Поскольку квадрат любого действительного числа не может быть отрицательным ($x^2 \ge 0$), то минимальное значение всего выражения достигается при $x = 0$ и равно $0 - 3 = -3$. Верхней границы нет.
**Итог:** $E(f) = [-3; +\infty)$
* **Пример 2 (Тригонометрическая функция):**
$f(x) = 2\cos(x) + 1$
Известно, что функция косинуса ограничена: $-1 \le \cos(x) \le 1$.
Умножаем все части неравенства на 2: $-2 \le 2\cos(x) \le 2$.
Прибавляем ко всем частям 1: $-1 \le 2\cos(x) + 1 \le 3$.
**Итог:** $E(f) = [-1; 3]$
* **Пример 3 (Дробно-рациональная функция):**
$f(x) = \frac{1}{x} + 2$
Дробь $\frac{1}{x}$ может принимать любые значения, кроме нуля (так как числитель не равен нулю). Следовательно, выражение $\frac{1}{x} + 2$ может быть любым числом, кроме $0 + 2 = 2$.
**Итог:** $E(f) = (-\infty; 2) \cup (2; +\infty)$



### Этап 3. Четность, нечетность и симметрия графика

**Объяснение:**
Исследование на четность и нечетность позволяет значительно упростить построение графика функции. Если функция обладает одним из этих свойств, достаточно исследовать и построить ее график только для положительных значений $x$ ($x \ge 0$), а затем отразить полученную кривую по правилам симметрии.

Функция проверяется путем подстановки $-x$ вместо $x$ в исходное уравнение:

1. **Четная функция:**
* **Условие:** $f(-x) = f(x)$ для любого $x$ из области определения.
* **Свойство графика:** График симметричен относительно вертикальной оси ординат ($Oy$). То есть правая и левая половины графика являются зеркальным отражением друг друга.


2. **Нечетная функция:**
* **Условие:** $f(-x) = -f(x)$ для любого $x$ из области определения.
* **Свойство графика:** График симметричен относительно начала координат (точки $O(0; 0)$). Если перевернуть график на 180 градусов, он совпадет сам с собой.


3. **Функция общего вида:**
* **Условие:** $f(-x) \neq f(x)$ и $f(-x) \neq -f(x)$.
* **Свойство графика:** Функция не обладает ни осевой, ни центральной симметрией. Большинство функций относятся именно к этому типу.



*(Важное примечание: Для того чтобы функция могла быть четной или нечетной, ее область определения $D(f)$ обязательно должна быть симметрична относительно нуля. Например, если функция определена на $[-5; 7]$, она сразу считается функцией общего вида).*

**Примеры:**

* **Пример 1 (Четная функция):**
$f(x) = x^4 - 2x^2$
Подставляем $-x$: $f(-x) = (-x)^4 - 2(-x)^2 = x^4 - 2x^2 = f(x)$.
**Итог:** Функция четная, график симметричен относительно оси $Oy$.
* **Пример 2 (Нечетная функция):**
$f(x) = x^3 - 5x$
Подставляем $-x$: $f(-x) = (-x)^3 - 5(-x) = -x^3 + 5x = -(x^3 - 5x) = -f(x)$.
**Итог:** Функция нечетная, график симметричен относительно начала координат.
* **Пример 3 (Функция общего вида):**
$f(x) = x^2 + 3x$
Подставляем $-x$: $f(-x) = (-x)^2 + 3(-x) = x^2 - 3x$.
Это выражение не равно ни $f(x)$, ни $-f(x)$.
**Итог:** Функция не является ни четной, ни нечетной.

### Лайфхаки: Быстрая проверка на четность и нечетность

| Ситуация | Паттерн | Пример |
| :--- | :--- | :--- |
| **Многочлен (полином)** | Все степени $x$ **четные** (число без $x$ — это $x^0$, четная степень) $\Rightarrow$ **четная**.<br>Все степени $x$ **нечетные** $\Rightarrow$ **нечетная**.<br>Есть и четные, и нечетные $\Rightarrow$ **общего вида**. | $f(x) = 3x^4 - x^2 + 7$ (степени 4, 2, 0) $\Rightarrow$ Четная<br>$f(x) = x^5 - 2x^3 + x$ (степени 5, 3, 1) $\Rightarrow$ Нечетная<br>$f(x) = x^3 + x^2$ (степени 3, 2) $\Rightarrow$ Общего вида |
| **Умножение / Деление** | Четная = $(+)$, Нечетная = $(-)$. Правило знаков:<br>$(+) \times (+) = (+)$ Четная<br>$(-) \times (-) = (+)$ Четная<br>$(+) \times (-) = (-)$ Нечетная | $f(x) = x \cdot \sin(x)$<br>Нечетная ($-$) $\cdot$ Нечетная ($-$) $\Rightarrow$ Четная<br>$f(x) = \frac{x^3}{\cos(x)}$<br>Нечетная ($-$) $/$ Четная ($+$) $\Rightarrow$ Нечетная |
| **Сложение / Вычитание** | Четная $\pm$ Четная = Четная<br>Нечетная $\pm$ Нечетная = Нечетная<br>Четная $\pm$ Нечетная = Общего вида | $f(x) = \cos(x) + x^2$<br>Четная $+$ Четная $\Rightarrow$ Четная<br>$f(x) = \cos(x) + x$<br>Четная $+$ Нечетная $\Rightarrow$ Общего вида |
| **Сложная функция $f(g(x))$** | Внутренняя $g(x)$ четная $\Rightarrow$ вся функция **четная**.<br>Внутренняя $g(x)$ нечетная $\Rightarrow$ четность совпадает с внешней $f(x)$. | $f(x) = \sin(x^2)$<br>Внутренняя $x^2$ четная $\Rightarrow$ Четная<br>$f(x) = \cos(x^3)$<br>Внутренняя $x^3$ нечетная, Внешняя $\cos$ четная $\Rightarrow$ Четная |
| **Модуль** | Взятие выражения в модуль "поглощает" минус, превращая функцию в **четную**. | $f(x) = \vert x \vert$ $\Rightarrow$ Четная<br>$f(x) = x^2 \cdot \vert \sin(x) \vert$<br>Четная $\cdot$ Четная $\Rightarrow$ Четная |



### Этап 4. Периодичность функции

**Объяснение:**
Периодическая функция — это функция, график которой бесконечно повторяется через равные промежутки вдоль оси $Ox$. Этот промежуток называется **периодом** и обозначается буквой $T$.

Суть этого свойства в экономии времени: если функция периодическая, нам не нужно исследовать её на всей числовой прямой. Достаточно построить график на одном отрезке длиной $T$ (например, от $0$ до $T$), а затем просто «скопировать и вставить» получившийся кусочек влево и вправо до бесконечности.

**Математическое условие:**
Функция периодична, если существует такое число $T \neq 0$, что для любого $x$ выполняется равенство:
$f(x - T) = f(x) = f(x + T)$

**Лайфхак для студентов:**
В классическом математическом анализе периодичность имеет смысл проверять **только** если в уравнении функции присутствуют тригонометрические элементы ($\sin, \cos, \text{tg}, \text{ctg}$) или дробная часть числа. Все обычные многочлены, дроби, корни и логарифмы периодичными не бывают. В таких случаях в исследовании просто пишется одна строчка: *"Функция не является периодической (апериодическая)"*.

**Примеры:**

* **Пример 1 (Классика):**
$f(x) = \sin(x)$
Базовый (наименьший положительный) период синуса и косинуса равен $2\pi$. Графики этих функций — бесконечные волны, где каждый «гребень» и «впадина» повторяются ровно через $2\pi$.
**Итог:** Периодическая, $T = 2\pi$.
* **Пример 2 (Изменение периода):**
$f(x) = \cos(3x)$
Коэффициент перед $x$ «сжимает» график. Чтобы найти новый период, нужно базовый период косинуса разделить на этот коэффициент.
$T = \frac{2\pi}{3}$
**Итог:** Периодическая, волна повторяется чаще.
* **Пример 3 (Обычная функция):**
$f(x) = x^2 + 5x$
Это парабола. Она не может повторяться, её ветви просто уходят вверх.
**Итог:** Апериодическая.

---

### Этап 5. Точки пересечения с осями координат (Нули функции)

**Объяснение:**
Чтобы построить точный график, нужно знать его «якоря» — точки, где кривая пересекает главные оси координат (крест осей $Ox$ и $Oy$).

1. **Пересечение с осью ординат ($Oy$):**
* **Правило:** В этой точке координата $x$ всегда равна $0$.
* **Действие:** Подставляем $x = 0$ в уравнение функции и вычисляем $y = f(0)$.
* *Важно:* Такая точка может быть только одна (иначе это не функция), либо её может не быть вообще (если $x=0$ не входит в область определения).


2. **Пересечение с осью абсцисс ($Ox$) — «Нули функции»:**
* **Правило:** В этих точках высота графика равна нулю, то есть $y = 0$.
* **Действие:** Приравниваем всю функцию к нулю: $f(x) = 0$ и решаем получившееся уравнение.
* *Важно:* Корней уравнения может быть несколько, один или ни одного (если график «висит» над осью или проходит под ней).



**Примеры:**

* **Пример 1 (Квадратичная функция):**
$f(x) = x^2 - 4x + 3$
* **С осью $Oy$ ($x=0$):** $y = 0^2 - 4 \cdot 0 + 3 = 3$. Точка **$(0; 3)$**.
* **С осью $Ox$ ($y=0$):** $x^2 - 4x + 3 = 0$. По теореме Виета корни $x_1 = 1$, $x_2 = 3$. Точки **$(1; 0)$** и **$(3; 0)$**.


* **Пример 2 (Рациональная функция):**
$f(x) = \frac{x - 2}{x}$
* **С осью $Oy$ ($x=0$):** Подставить нельзя (деление на ноль). График **не пересекает** ось $Oy$ (там находится вертикальная асимптота).
* **С осью $Ox$ ($y=0$):** Дробь равна нулю, когда её числитель равен нулю: $x - 2 = 0 \Rightarrow x = 2$. Точка **$(2; 0)$**.


* **Пример 3 (Нет нулей):**
$f(x) = x^2 + 5$
* **С осью $Oy$ ($x=0$):** $y = 5$. Точка **$(0; 5)$**.
* **С осью $Ox$ ($y=0$):** $x^2 + 5 = 0 \Rightarrow x^2 = -5$. Уравнение не имеет действительных корней. График **не пересекает** ось $Ox$.


### Этап 6. Интервалы знакопостоянства

**Объяснение:**
Интервалы знакопостоянства — это промежутки на оси $Ox$, на которых график функции располагается строго выше оси абсцисс ($f(x) > 0$) или строго ниже оси абсцисс ($f(x) < 0$).

Алгоритм нахождения (метод интервалов):
1. Найти нули функции (где $f(x) = 0$) и точки разрыва (где функция не определена, например, нули знаменателя).
2. Нанести эти точки на числовую прямую. Прямая разобьется на интервалы.
3. Выбрать по одному контрольному числу из каждого интервала, подставить в исходное уравнение функции и определить знак результата («+» или «-»).
4. Записать ответ в виде объединения промежутков.

**Примеры:**

*   **Пример 1 (Многочлен):** 
    $f(x) = x^2 - 9$
    Нули функции: $x^2 - 9 = 0 \Rightarrow x = -3, x = 3$.
    Наносим на прямую, получаем три интервала: $(-\infty; -3)$, $(-3; 3)$, $(3; +\infty)$.
    Проверяем знаки:
    На $(-3; 3)$ берем $x=0$: $0^2 - 9 = -9$ (знак «-», график ниже оси).
    На $(3; +\infty)$ берем $x=4$: $4^2 - 9 = 7$ (знак «+», график выше оси).
    На $(-\infty; -3)$ берем $x=-4$: $(-4)^2 - 9 = 7$ (знак «+», график выше оси).
    **Итог:** 
    $f(x) > 0$ при $x \in (-\infty; -3) \cup (3; +\infty)$
    $f(x) < 0$ при $x \in (-3; 3)$

*   **Пример 2 (Дробно-рациональная функция):** 
    $f(x) = \frac{x - 2}{x + 1}$
    Нуль функции (числитель равен $0$): $x = 2$.
    Точка разрыва (знаменатель равен $0$): $x = -1$.
    Наносим точки $-1$ (выколотая) и $2$ на прямую.
    Интервалы: $(-\infty; -1)$, $(-1; 2)$, $(2; +\infty)$.
    Проверяем знаки (подставляем числа $-2, 0, 3$): чередование +, -, +.
    **Итог:**
    $f(x) > 0$ при $x \in (-\infty; -1) \cup (2; +\infty)$
    $f(x) < 0$ при $x \in (-1; 2)$


### Этап 7. Непрерывность и точки разрыва

**Объяснение:**
Функция называется непрерывной в точке, если предел функции в этой точке равен её значению ( $\lim_{x\to a} f(x) = f(a)$ ). На практике это означает, что график функции представляет собой сплошную линию без "дырок" и скачков.

Точки, в которых непрерывность нарушается (функция не определена или пределы не совпадают), называются **точками разрыва**. Они классифицируются по трем типам на основе вычисления односторонних пределов (слева $\lim_{x \to a-} f(x)$ и справа $\lim_{x \to a+} f(x)$ ):

1. **Устранимый разрыв:**
   * Односторонние пределы конечны и равны друг другу, но функция в этой точке не определена или её значение отличается от предела.
   * Визуально: "выколотая" точка на графике.
2. **Разрыв I рода (скачок):**
   * Односторонние пределы конечны, но не равны друг другу.
   * Визуально: график "прыгает" на другой уровень.
3. **Разрыв II рода (бесконечный):**
   * Хотя бы один из односторонних пределов равен бесконечности ($\pm\infty$) или не существует.
   * Визуально: график уходит вдоль вертикальной асимптоты вверх или вниз.

**Примеры:**

*   **Пример 1 (Устранимый разрыв):** 
    $f(x) = \frac{x^2 - 4}{x - 2}$
    Функция не определена в точке $x = 2$. Однако предел при $x \to 2$ равен $4$. График представляет собой прямую $y = x + 2$ с выколотой точкой $(2; 4)$.

*   **Пример 2 (Разрыв I рода):** 
    Сигнум-функция $f(x) = \text{sgn}(x)$ (или $y = \frac{|x|}{x}$).
    При $x < 0$ значение равно $-1$, при $x > 0$ значение равно $1$. В точке $x = 0$ функция терпит разрыв со скачком. Предел слева равен $-1$, предел справа равен $1$.

*   **Пример 3 (Разрыв II рода):** 
    $f(x) = \frac{1}{x - 3}$
    В точке $x = 3$ знаменатель обращается в ноль. При стремлении $x$ к $3$ слева предел равен $-\infty$, справа — $+\infty$. Точка $x = 3$ — разрыв II рода (вертикальная асимптота).


```
