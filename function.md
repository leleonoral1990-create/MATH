### СЛАЙД 1: Зачем ML-инженеру исследовать функции?

1. **Область определения $D(y)$:** * *ML:* Защита от системных ошибок (`NaN`). 
   * *Пример:* Log Loss требует вероятности $p > 0$.
2. **Область значения $E(f)$:** * *ML:* Приведение прогнозов в нужный формат. 
   * *Пример:* Сигмоида сжимает сырые ответы в проценты от 0 до 1.
3. **Четность / нечетность:** * *ML:* Ускорение обучения через центрирование данных. 
   * *Пример:* Нечетная функция Tanh сходится быстрее базовой Сигмоиды.
4. **Периодичность:** * *ML:* Кодирование цикличных признаков. 
   * *Пример:* Использование `sin`/`cos` для объяснения алгоритму времени суток.
5. **Точки пересечения с осями:** * *ML:* Базовое предсказание по умолчанию (bias). 
   * *Пример:* Смещение $b$ в уравнении регрессии.
6. **Интервалы знакопостоянства:** * *ML:* Фильтрация полезного сигнала. 
   * *Пример:* ReLU ($y = \max(0, x)$) обнуляет негативные сигналы, экономя память.
7. **Непрерывность и разрывы:** * *ML:* Возможность обучать сеть. 
   * *Пример:* Ступенчатая функция не имеет производной в разрыве, ИИ на ней не учится.
8. **Асимптоты:** * *ML:* Проблема "затухания градиента" (паралич сети). 
   * *Пример:* На концах Сигмоиды наклон равен нулю, веса перестают обновляться.
9. **Монотонность и экстремумы (1-я производная):** * *ML:* Поиск оптимальных настроек. 
   * *Пример:* Градиентный спуск ищет минимум (экстремум) ошибки.
10. **Выпуклость и перегибы (2-я производная):** * *ML:* Оценка риска застрять в плохом решении. 
    * *Пример:* Выпуклые функции имеют 1 глобальное дно (легко найти). Функции с перегибами имеют обманчивые локальные минимумы.
11. **Построение графика:** * *ML:* Визуальный контроль за "здоровьем" модели. 
    * *Пример:* Графики кривых обучения (Learning Curves) помогают избежать переобучения.

### 2 СЛАЙД: Главное правило функции

**Базовое правило математики:**
У одного $x$ **не может** быть нескольких $y$. Каждому $x$ соответствует строго один единственный результат. Если результатов больше — это геометрическая фигура, а не функция.

**Как проверить визуально? (Тест вертикальной линии):**
*

* **Функция:** Вертикальная линия пересекает график только в 1 точке (например, парабола).
* **НЕ функция:** Вертикальная линия протыкает график в 2 и более точках (например, окружность).

**Метафора (Торговый автомат):**
Если нажать кнопку «А1» ($x$), автомат должен выдать «Сникерс» ($y$). Если при нажатии «А1» выпадает то Сникерс, то вода — автомат (функция) сломан.

**Как это работает в Machine Learning?**

* **Проблема (Шум в данных):** В датасете могут быть два пассажира с абсолютно одинаковыми признаками ($x$), но один выжил ($y=1$), а другой нет ($y=0$).
* **Реакция ИИ:** Алгоритм не может выдать два ответа. Он пойдет на компромисс и усреднит их, выдав вероятность **50%**.
* **Решение аналитика:** Если модель часто выдает 50%, значит в данных есть противоречия. Нужно **добавить новые признаки** (например, колонку «Был ли спасательный жилет?»), чтобы сделать каждый $x$ уникальным.

### СЛАЙД 3. Этап 1. Область определения функции $D(f)$

**Объяснение:**
Это множество всех значений переменной $x$, при которых функция «существует» (ее можно вычислить). Мы ищем «запретные зоны».

* **Дробь:** знаменатель не равен 0.
* **Корень четной степени:** подкоренное выражение $\ge 0$.
* **Логарифм:** аргумент $> 0$.

**Пример:**
Для функции $f(x) = \frac{5}{x - 3} + \sqrt{x + 1}$:

1. $x - 3 \neq 0 \Rightarrow x \neq 3$.
2. $x + 1 \ge 0 \Rightarrow x \ge -1$.
**Итог:** $D(f) = [-1; 3) \cup (3; +\infty)$.


### СЛАЙД 4. Этап 2. Область значений функции $E(f)$

**Объяснение:**
Область значений функции (множество значений) — это все возможные значения, которые может принимать зависимая переменная $y$ (функция) при переборе всех допустимых значений независимой переменной $x$ из области определения. Геометрически область значений представляет собой проекцию всего графика функции на вертикальную ось ординат ($Oy$).

В то время как область определения показывает, где график существует по горизонтали, область значений показывает, какие границы график имеет по вертикали.

Основные способы нахождения $E(f)$:

1. **Оценка алгебраических выражений:** использование базовых свойств (любой квадрат $\ge 0$, модуль $\ge 0$, корень $\ge 0$).
2. **Свойства тригонометрических функций:** использование встроенных ограничений (например, $-1 \le \sin(x) \le 1$).
3. **Использование производной:** нахождение абсолютных максимумов и минимумов функции на ее области определения.
4. **Выражение $x$ через $y$:** решение уравнения $y = f(x)$ относительно $x$ для поиска ограничений на $y$.

**Примеры:**

* **Пример 1 (Квадратичная функция):**
$f(x) = x^2 - 3$
Поскольку квадрат любого действительного числа не может быть отрицательным ($x^2 \ge 0$), то минимальное значение всего выражения достигается при $x = 0$ и равно $0 - 3 = -3$. Верхней границы нет.
**Итог:** $E(f) = [-3; +\infty)$
* **Пример 2 (Тригонометрическая функция):**
$f(x) = 2\cos(x) + 1$
Известно, что функция косинуса ограничена: $-1 \le \cos(x) \le 1$.
Умножаем все части неравенства на 2: $-2 \le 2\cos(x) \le 2$.
Прибавляем ко всем частям 1: $-1 \le 2\cos(x) + 1 \le 3$.
**Итог:** $E(f) = [-1; 3]$
* **Пример 3 (Дробно-рациональная функция):**
$f(x) = \frac{1}{x} + 2$
Дробь $\frac{1}{x}$ может принимать любые значения, кроме нуля (так как числитель не равен нулю). Следовательно, выражение $\frac{1}{x} + 2$ может быть любым числом, кроме $0 + 2 = 2$.
**Итог:** $E(f) = (-\infty; 2) \cup (2; +\infty)$



### СЛАЙД 5. Этап 3. Четность, нечетность и симметрия графика

**Объяснение:**
Исследование на четность и нечетность позволяет значительно упростить построение графика функции. Если функция обладает одним из этих свойств, достаточно исследовать и построить ее график только для положительных значений $x$ ($x \ge 0$), а затем отразить полученную кривую по правилам симметрии.

Функция проверяется путем подстановки $-x$ вместо $x$ в исходное уравнение:

1. **Четная функция:**
* **Условие:** $f(-x) = f(x)$ для любого $x$ из области определения.
* **Свойство графика:** График симметричен относительно вертикальной оси ординат (Oy). То есть правая и левая половины графика являются зеркальным отражением друг друга.


2. **Нечетная функция:**
* **Условие:** $f(-x) = -f(x)$ для любого $x$ из области определения.
* **Свойство графика:** График симметричен относительно начала координат (точки $O(0; 0)$ ). Если перевернуть график на 180 градусов, он совпадет сам с собой.


3. **Функция общего вида:**
* **Условие:** $f(-x) \neq f(x)$ и $f(-x) \neq -f(x)$.
* **Свойство графика:** Функция не обладает ни осевой, ни центральной симметрией. Большинство функций относятся именно к этому типу.



*(Важное примечание: Для того чтобы функция могла быть четной или нечетной, ее область определения $D(f)$ обязательно должна быть симметрична относительно нуля. Например, если функция определена на $[-5; 7]$, она сразу считается функцией общего вида).*

**Примеры:**

* **Пример 1 (Четная функция):**
$f(x) = x^4 - 2x^2$
Подставляем $-x$: $f(-x) = (-x)^4 - 2(-x)^2 = x^4 - 2x^2 = f(x)$.
**Итог:** Функция четная, график симметричен относительно оси $Oy$.
* **Пример 2 (Нечетная функция):**
$f(x) = x^3 - 5x$
Подставляем $-x$: $f(-x) = (-x)^3 - 5(-x) = -x^3 + 5x = -(x^3 - 5x) = -f(x)$.
**Итог:** Функция нечетная, график симметричен относительно начала координат.
* **Пример 3 (Функция общего вида):**
$f(x) = x^2 + 3x$
Подставляем $-x$: $f(-x) = (-x)^2 + 3(-x) = x^2 - 3x$.
Это выражение не равно ни $f(x)$, ни $-f(x)$.
**Итог:** Функция не является ни четной, ни нечетной.

### Лайфхаки: Быстрая проверка на четность и нечетность

**Лайфхаки (Быстрые проверки):**

1. **По степеням (Только для многочленов):**
   * Все степени четные (2, 4, 6 + числа) = **Четная**.
   * Все степени нечетные (1, 3, 5) = **Нечетная**.
   * Смесь степеней = **Общего вида**.

2. **По арифметике (Как плюс и минус):**
   * Нечетная $\times$ Нечетная = **Четная**.
   * Четная $\times$ Нечетная = **Нечетная**.

3. **По тригонометрии:** * $\cos(x)$ — **четная**, остальные ( $\sin(x), \tan(x)$ ) — **нечетные**.

---

**Ограничения и Капканы (Когда лайфхаки не работают):**

* **Асимметрия области $D(f)$:** Если график ограничен (например, анализируем только положительные цены или возраст), функция *всегда* общего вида, какие бы красивые степени у нее ни были.
* **Смещенный центр:** Парабола $y = (x-2)^2$ визуально симметрична, но математически — общего вида, так как съехала с центра координат.
* **Скрытые свойства:** Логарифмы, сложные дроби и модули нельзя проверить лайфхаками. Нужно вручную подставлять $f(-x)$ в формулу.
* **Уникальное исключение:** Функция $y = 0$ является четной и нечетной одновременно.




### Этап 4. Периодичность функции

**Объяснение:**
Периодическая функция — это функция, график которой бесконечно повторяется через равные промежутки вдоль оси $Ox$. Этот промежуток называется **периодом** и обозначается буквой $T$.

Суть этого свойства в экономии времени: если функция периодическая, нам не нужно исследовать её на всей числовой прямой. Достаточно построить график на одном отрезке длиной $T$ ( например, от $0$ до $T$ ), а затем просто «скопировать и вставить» получившийся кусочек влево и вправо до бесконечности.

**Математическое условие:**
Функция периодична, если существует такое число $T \neq 0$, что для любого $x$ выполняется равенство:
$f(x - T) = f(x) = f(x + T)$

**Лайфхак для студентов:**
В классическом математическом анализе периодичность имеет смысл проверять **только** если в уравнении функции присутствуют тригонометрические элементы ( $\sin, \cos, \text{tg}, \text{ctg}$ ) или дробная часть числа. Все обычные многочлены, дроби, корни и логарифмы периодичными не бывают. В таких случаях в исследовании просто пишется одна строчка: *"Функция не является периодической (апериодическая)"*.

**Примеры:**

* **Пример 1 (Классика):**
$f(x) = \sin(x)$
Базовый (наименьший положительный) период синуса и косинуса равен $2\pi$. Графики этих функций — бесконечные волны, где каждый «гребень» и «впадина» повторяются ровно через $2\pi$.
**Итог:** Периодическая, $T = 2\pi$.
* **Пример 2 (Изменение периода):**
$f(x) = \cos(3x)$
Коэффициент перед $x$ «сжимает» график. Чтобы найти новый период, нужно базовый период косинуса разделить на этот коэффициент.
$T = \frac{2\pi}{3}$
**Итог:** Периодическая, волна повторяется чаще.
* **Пример 3 (Обычная функция):**
$f(x) = x^2 + 5x$
Это парабола. Она не может повторяться, её ветви просто уходят вверх.
**Итог:** Апериодическая.

---

### Этап 5. Точки пересечения с осями координат (Нули функции)

**Объяснение:**
Чтобы построить точный график, нужно знать его «якоря» — точки, где кривая пересекает главные оси координат ( крест осей $Ox$ и $Oy$ ).

1. **Пересечение с осью ординат ( $Oy$ ):**
* **Правило:** В этой точке координата $x$ всегда равна $0$.
* **Действие:** Подставляем $x = 0$ в уравнение функции и вычисляем $y = f(0)$.
* *Важно:* Такая точка может быть только одна (иначе это не функция), либо её может не быть вообще (если $x=0$ не входит в область определения).


2. **Пересечение с осью абсцисс ( $Ox$ ) — «Нули функции»:**
* **Правило:** В этих точках высота графика равна нулю, то есть $y = 0$.
* **Действие:** Приравниваем всю функцию к нулю: $f(x) = 0$ и решаем получившееся уравнение.
* *Важно:* Корней уравнения может быть несколько, один или ни одного (если график «висит» над осью или проходит под ней).



**Примеры:**

* **Пример 1 (Квадратичная функция):**
$f(x) = x^2 - 4x + 3$
* **С осью $Oy$ ( $x=0$ ):** $y = 0^2 - 4 \cdot 0 + 3 = 3$. Точка **$(0; 3)$**.
* **С осью $Ox$ ( $y=0$ ):** $x^2 - 4x + 3 = 0$. По теореме Виета корни $x_1 = 1$, $x_2 = 3$. Точки **$(1; 0)$** и **$(3; 0)$**.


* **Пример 2 (Рациональная функция):**
$f(x) = \frac{x - 2}{x}$
* **С осью $Oy$ ($x=0$ ):** Подставить нельзя (деление на ноль). График **не пересекает** ось $Oy$ (там находится вертикальная асимптота).
* **С осью $Ox$ ($y=0$ ):** Дробь равна нулю, когда её числитель равен нулю: $x - 2 = 0 \Rightarrow x = 2$. Точка **$(2; 0)$**.


* **Пример 3 (Нет нулей):**
$f(x) = x^2 + 5$
* **С осью $Oy$ ($x=0$ ):** $y = 5$. Точка **$(0; 5)$**.
* **С осью $Ox$ ($y=0$ ):** $x^2 + 5 = 0 \Rightarrow x^2 = -5$. Уравнение не имеет действительных корней. График **не пересекает** ось $Ox$.


### Этап 6. Интервалы знакопостоянства

**Объяснение:**
Интервалы знакопостоянства — это промежутки на оси $Ox$, на которых график функции располагается строго выше оси абсцисс ($f(x) > 0$ ) или строго ниже оси абсцисс ($f(x) < 0$ ).

Алгоритм нахождения (метод интервалов):
1. Найти нули функции (где $f(x) = 0$ ) и точки разрыва (где функция не определена, например, нули знаменателя).
2. Нанести эти точки на числовую прямую. Прямая разобьется на интервалы.
3. Выбрать по одному контрольному числу из каждого интервала, подставить в исходное уравнение функции и определить знак результата («+» или «-»).
4. Записать ответ в виде объединения промежутков.

**Примеры:**

*   **Пример 1 (Многочлен):** 
    $f(x) = x^2 - 9$
    Нули функции: $x^2 - 9 = 0 \Rightarrow x = -3, x = 3$.
    Наносим на прямую, получаем три интервала: $(-\infty; -3)$, $(-3; 3)$, $(3; +\infty)$.
    Проверяем знаки:
    На $(-3; 3)$ берем $x=0$: $0^2 - 9 = -9$ (знак «-», график ниже оси).
    На $(3; +\infty)$ берем $x=4$: $4^2 - 9 = 7$ (знак «+», график выше оси).
    На $(-\infty; -3)$ берем $x=-4$: $(-4)^2 - 9 = 7$ (знак «+», график выше оси).
    **Итог:** 
    $f(x) > 0$ при $x \in (-\infty; -3) \cup (3; +\infty)$
    $f(x) < 0$ при $x \in (-3; 3)$

*   **Пример 2 (Дробно-рациональная функция):** 
    $f(x) = \frac{x - 2}{x + 1}$
    Нуль функции (числитель равен $0$ ): $x = 2$.
    Точка разрыва (знаменатель равен $0$ ): $x = -1$.
    Наносим точки $-1$ (выколотая) и $2$ на прямую.
    Интервалы: $(-\infty; -1)$, $(-1; 2)$, $(2; +\infty)$.
    Проверяем знаки (подставляем числа $-2, 0, 3$ ): чередование +, -, +.
    **Итог:**
    $f(x) > 0$ при $x \in (-\infty; -1) \cup (2; +\infty)$
    $f(x) < 0$ при $x \in (-1; 2)$


### Этап 7. Непрерывность и точки разрыва

**Объяснение:**
Функция называется непрерывной в точке, если предел функции в этой точке равен её значению ( $\lim_{x\to a} f(x) = f(a)$ ). На практике это означает, что график функции представляет собой сплошную линию без "дырок" и скачков.

Точки, в которых непрерывность нарушается (функция не определена или пределы не совпадают), называются **точками разрыва**. Они классифицируются по трем типам на основе вычисления односторонних пределов (слева $\lim_{x \to a-} f(x)$ и справа $\lim_{x \to a+} f(x)$ ):

1. **Устранимый разрыв:**
   * Односторонние пределы конечны и равны друг другу, но функция в этой точке не определена или её значение отличается от предела.
   * Визуально: "выколотая" точка на графике.
2. **Разрыв I рода (скачок):**
   * Односторонние пределы конечны, но не равны друг другу.
   * Визуально: график "прыгает" на другой уровень.
3. **Разрыв II рода (бесконечный):**
   * Хотя бы один из односторонних пределов равен бесконечности ($\pm\infty$) или не существует.
   * Визуально: график уходит вдоль вертикальной асимптоты вверх или вниз.

**Примеры:**

*   **Пример 1 (Устранимый разрыв):** 
    $f(x) = \frac{x^2 - 4}{x - 2}$
    Функция не определена в точке $x = 2$. Однако предел при $x \to 2$ равен $4$. График представляет собой прямую $y = x + 2$ с выколотой точкой $(2; 4)$.

*   **Пример 2 (Разрыв I рода):** 
    Сигнум-функция $f(x) = \text{sgn}(x)$ (или $y = \frac{|x|}{x}$).
    При $x < 0$ значение равно $-1$, при $x > 0$ значение равно $1$. В точке $x = 0$ функция терпит разрыв со скачком. Предел слева равен $-1$, предел справа равен $1$.

*   **Пример 3 (Разрыв II рода):** 
    $f(x) = \frac{1}{x - 3}$
    В точке $x = 3$ знаменатель обращается в ноль. При стремлении $x$ к $3$ слева предел равен $-\infty$, справа — $+\infty$. Точка $x = 3$ — разрыв II рода (вертикальная асимптота).


### Этап 8. Асимптоты графика функции

**Объяснение:**
Асимптота — это воображаемая прямая линия, к которой график функции приближается бесконечно близко по мере удаления в бесконечность или при приближении к определенным точкам, но никогда её не пересекает (по крайней мере, на своих "краях"). Это своеобразные «границы» или «направляющие» для функции.

Асимптоты делятся на три вида:

1.  **Вертикальные асимптоты ($x = a$):**
    *   **Суть:** График улетает в бесконечность (вверх или вниз) вдоль вертикальной линии. Возникают в точках разрыва II рода.
    *   **Паттерн:** Чаще всего это точки, в которых знаменатель дроби равен нулю, а числитель не равен нулю (или границы области определения логарифмов).
    *   **Как доказать:** Вычислить односторонние пределы в подозреваемой точке $a$. Если $\lim_{x \to a} f(x) = \pm\infty$, то прямая $x = a$ — вертикальная асимптота.

2.  **Горизонтальные асимптоты ($y = b$):**
    *   **Суть:** Показывают, к какой высоте ($y$) стремится график, если двигаться по оси $x$ бесконечно далеко вправо ($+\infty$) или влево ($-\infty$).
    *   **Как доказать:** Вычислить пределы на бесконечности: $b = \lim_{x \to \pm\infty} f(x)$. Если в результате получается конкретное число $b$, то прямая $y = b$ — горизонтальная асимптота.

3.  **Наклонные асимптоты ($y = kx + b$):**
    *   **Суть:** График на бесконечности вытягивается вдоль диагональной прямой. 
    *   *Лайфхак:* Горизонтальная асимптота — это частный случай наклонной (когда наклон $k = 0$). Если у функции есть горизонтальная асимптота при $x \to +\infty$, то наклонной с этой же стороны уже точно не будет.
    *   **Как доказать:** Необходимо найти два коэффициента через пределы:
        1.  $k = \lim_{x \to \pm\infty} \frac{f(x)}{x}$ (Если $k = \infty$ или не существует, наклонной асимптоты нет).
        2.  $b = \lim_{x \to \pm\infty} (f(x) - kx)$

**Примеры:**

*   **Пример 1 (Вертикальная и горизонтальная):** 
    $f(x) = \frac{2x}{x - 1}$
    *   *Вертикальная:* Знаменатель равен нулю при $x = 1$. Предел в этой точке равен $\infty$. Асимптота: **$x = 1$**.
    *   *Горизонтальная:* $\lim_{x \to \pm\infty} \frac{2x}{x - 1} = 2$. Асимптота: **$y = 2$**.

*   **Пример 2 (Вертикальная и наклонная):** 
    $f(x) = \frac{x^2 + 1}{x}$
    *   *Вертикальная:* Деление на ноль при $x = 0$. Асимптота: **$x = 0$** (совпадает с осью $Oy$).
    *   *Наклонная:* 
        $k = \lim_{x \to \pm\infty} \frac{x^2+1}{x \cdot x} = 1$. 
        $b = \lim_{x \to \pm\infty} \left(\frac{x^2+1}{x} - 1 \cdot x\right) = \lim_{x \to \pm\infty} \frac{1}{x} = 0$. 
        Уравнение: $y = 1 \cdot x + 0$. Асимптота: **$y = x$**.


 ### Этап 9. Монотонность и экстремумы (Первая производная)

**Объяснение:**
Первая производная $f'(x)$ показывает скорость изменения функции. Она определяет интервалы монотонности (участки, где график только растет или только падает) и точки экстремума (локальные вершины и впадины).

**Алгоритм исследования:**
1. Найти область определения функции $D(f)$.
2. Вычислить первую производную $f'(x)$.
3. Найти критические точки: приравнять производную к нулю ($f'(x) = 0$) и выявить точки, где производная не существует.
4. Отметить критические точки на числовой прямой и разбить область определения на интервалы.
5. Определить знак производной $f'(x)$ на каждом интервале, подставив контрольное число из интервала в уравнение производной.

**Анализ первой производной**

| Ситуация | Паттерн (Условие) | Пример |
| :--- | :--- | :--- |
| **Функция возрастает** | $f'(x) > 0$ на интервале. График идет вверх. | $f'(x) = 3$. Значение положительное, функция растет. |
| **Функция убывает** | $f'(x) < 0$ на интервале. График идет вниз. | $f'(x) = -2$. Значение отрицательное, функция падает. |
| **Точка максимума ($x_{max}$)** | Знак $f'(x)$ меняется с «$+$» на «$-$» при переходе через критическую точку (рост сменяется падением). | $f'(x)$ меняет знак в точке $x = -1$ с плюса на минус. Точка $x = -1$ — локальный максимум (вершина холма). |
| **Точка минимума ($x_{min}$)** | Знак $f'(x)$ меняется с «$-$» на «$+$» при переходе через критическую точку (падение сменяется ростом). | $f'(x)$ меняет знак в точке $x = 1$ с минуса на плюс. Точка $x = 1$ — локальный минимум (дно впадины). |
| **Критическая точка без экстремума** | Знак $f'(x)$ не меняется (например, с «$+$» на «$+$»). Функция продолжает рост или падение. | $f(x) = x^3$, $f'(x) = 3x^2$. В точке $x = 0$ производная равна нулю, но знак слева и справа положительный. Рост продолжается. |

**Полный пример:**
Дана функция $f(x) = x^3 - 3x$.
1. Производная: $f'(x) = 3x^2 - 3$.
2. Критические точки: $3x^2 - 3 = 0 \Rightarrow 3(x^2 - 1) = 0 \Rightarrow x = 1, x = -1$.
3. Наносим точки $-1$ и $1$ на прямую, получаем интервалы: $(-\infty; -1)$, $(-1; 1)$, $(1; +\infty)$.
4. Знаки производной $f'(x) = 3(x^2 - 1)$:
   * На $(-\infty; -1)$ подставляем $x = -2$: $3((-2)^2 - 1) = 9$ ($+$). Функция возрастает.
   * На $(-1; 1)$ подставляем $x = 0$: $3(0^2 - 1) = -3$ ($-$). Функция убывает.
   * На $(1; +\infty)$ подставляем $x = 2$: $3(2^2 - 1) = 9$ ($+$). Функция возрастает.
5. Выводы по экстремумам:
   * В точке $x = -1$ знак меняется с «$+$» на «$-$» $\Rightarrow$ $x_{max} = -1$.
   * В точке $x = 1$ знак меняется с «$-$» на «$+$» $\Rightarrow$ $x_{min} = 1$.
  
### Этап 10. Выпуклость, вогнутость и точки перегиба (Вторая производная)

**Объяснение:**
Если первая производная показывает *скорость* изменения функции (рост/падение), то вторая производная $f''(x)$ показывает *ускорение* — то, как именно изгибается график. Это позволяет определить форму кривой.

**Алгоритм исследования:**
1. Найти вторую производную $f''(x)$ (взять производную от первой производной).
2. Приравнять её к нулю: $f''(x) = 0$. Решения этого уравнения — это "подозреваемые" точки перегиба.
3. Отметить эти точки на числовой прямой и определить знак $f''(x)$ на каждом из полученных интервалов.

**Правила знаков (Паттерны):**

| Знак $f''(x)$ | Свойство графика | Визуальная ассоциация |
| :--- | :--- | :--- |
| **$f''(x) > 0$** | График **вогнутый** (направлен выпуклостью вниз). | Форма «чаши» или «улыбающегося смайлика» ($\cup$). |
| **$f''(x) < 0$** | График **выпуклый** (направлен выпуклостью вверх). | Форма «купола» или «грустного смайлика» ($\cap$). |
| **Смена знака** | **Точка перегиба**. | Место сцепки двух дуг, где «чаша» плавно переходит в «купол» (или наоборот). |

**Пример:**
Дана функция $f(x) = x^3$.
1. Первая производная: $f'(x) = 3x^2$.
2. Вторая производная: $f''(x) = 6x$.
3. Ищем перегиб: $6x = 0 \Rightarrow x = 0$.
4. Анализируем интервалы:
   * При $x < 0$ (например, $x = -1$): $f''(-1) = -6$ (знак «$-$»). График **выпуклый** (купол).
   * При $x > 0$ (например, $x = 1$): $f''(1) = 6$ (знак «$+$»). График **вогнутый** (чаша).
5. **Вывод:** Так как при переходе через точку $x = 0$ вторая производная меняет знак с минуса на плюс, точка с координатами $(0; 0)$ является **точкой перегиба**.

